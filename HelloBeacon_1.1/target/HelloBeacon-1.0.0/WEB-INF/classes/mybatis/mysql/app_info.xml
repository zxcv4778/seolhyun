<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="appInfo">
	<select id="selectAppInfo" parameterType="hashmap" resultType="hashmap">
		SELECT
			IOS_APP_VERSION AS iosAppVersion,
			IOS_STORE_ADDRESS as iosStoreAddress,
			ANDROID_APP_VERSION AS androidAppVersion,
			ANDROID_STORE_ADDRESS as androidStoreAddress
		FROM
			APP_INFO
		LIMIT 1
	</select>
</mapper>